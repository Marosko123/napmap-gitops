apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: napmap-webapi
  namespace: wac-hospital
spec:
  imageRepositoryRef:
    name: napmap-webapi
  filterTags:
    pattern: "main.*"
  policy:
    alphabetical:
      order: asc
