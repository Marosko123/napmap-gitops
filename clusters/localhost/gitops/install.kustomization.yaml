apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: mb-napmap-install
  namespace: wac-hospital
spec:
  wait: true
  force: true
  dependsOn:
    - name: mb-napmap-prepare
  interval: 120s
  path: clusters/localhost/install
  prune: true
  sourceRef:
    kind: GitRepository
    name: mb-napmap-gitops-repo
